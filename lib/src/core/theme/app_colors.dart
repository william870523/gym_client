import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF135BEC);
  static const Color primaryHover = Color(0xFF0E4BCE);

  // Backgrounds
  static const Color lightBackground = Color(0xFFF6F6F8);
  static const Color darkBackground = Color(0xFF101622);

  // Surfaces (Cards, Panels)
  static const Color lightSurface = Colors.white;
  static const Color darkSurface = Color(0xFF1A202C);

  // Borders
  static const Color lightBorder = Color(0xFFE7EBF3);
  static const Color darkBorder = Color(0xFF2D3748);

  // Text
  static const Color lightTextPrimary = Color(0xFF0D121B);
  static const Color darkTextPrimary = Colors.white;
  static const Color lightTextSecondary = Color(0xFF4C669A);
  static const Color darkTextSecondary = Color(0xFF94A3B8);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
}
